# 📜 前端单页应用 (SPA) 页面生命周期与缓存失效规范
> **适用范围**: React / Vue / 现代前端工程

---

## 1. 核心流程与阶段
1. **初始化与路由守卫**：校验登录态与 RBAC 权限，未通过前阻断页面渲染；
2. **视口动态懒加载**：首屏关键视口优先渲染，非首屏或折叠图表使用 `IntersectionObserver` 延缓加载；
3. **页面卸载清理**：页面注销时必须自动清除轮询 Timer、注销 WebSocket 监听与解除 DOM 事件。
